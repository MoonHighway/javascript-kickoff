window.onload = function () {

    document.body.appendChild(createTable(may, "May Transactions"));
    document.body.appendChild(createTable(favColors, "Favorite Colors"));

};

function createTable(objs, captionText) {

    //
    // TODO: Code a Create Table Function that dynamically genrates a table
    //

}